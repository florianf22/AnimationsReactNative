import * as React from 'react';
import { Image, Text, View } from 'react-native';

export default () => {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: '#fff',
      }}
    ></View>
  );
};
