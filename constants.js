import { Dimensions } from 'react-native';

export const { height, width } = Dimensions.get('window');

export const spacing = 20;

export const cardWidth = width * 0.9;
export const cardHeight = height * 0.18;
